{
  "name": "fanout-bidding-demo-compute",
  "version": "1.0.0",
  "description": "Fastly Compute service for Fanout Bidding Demo",
  "main": "src/index.js",
  "type": "module",
  "scripts": {
    "generate-statics": "node scripts/generate-statics.js",
    "prebuild": "npm run generate-statics && npx esbuild src/index.js --bundle --outfile=bin/index.js --format=esm --platform=neutral --target=es2022 --external:fastly:*",
    "build": "js-compute-runtime bin/index.js bin/main.wasm"
  },
  "devDependencies": {
    "@fastly/js-compute": "^3.0.0",
    "esbuild": "^0.20.0"
  }
}
